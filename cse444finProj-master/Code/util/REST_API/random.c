#include <stdio.h>
#include <stdlib.h>

/* int timesTwo(int x){
    return x*2;
} */

int main(void){
  //char buff[100];
  //int input,output;
  //while(1){
    //gets(buff);
    //input = atoi(buff);
    //output = timesTwo(input);
    printf("Hello world! From c!");
    // you must flush stdout or else node will hang!
    fflush(stdout);
  //}
  return 0;
}